# REQUIREMENTS — 需求(做什么 / 为什么 / 怎样算完成)

> 本文件随用户逐步说明而填充。当前**绝大部分待定**,先列出待回答的问题骨架,谈到一条就钉一条。
> 规则:已确认的写成肯定句并标日期;未确认的放「待确认」区,别当成既定事实用。

## 1. 目标(一句话)
- 在 M5Stack Unit-C6L(ESP32-C6)上做一个**对讲机 / Walkie-Talkie**。*(细节待用户说明)*

## 2. 使用场景与形态
- 设备数量 / 拓扑:点对点?一对多?组网?——**待定**
- 使用距离 / 环境:——**待定**
- 供电与外形:电池?外壳?按键 PTT?——**待定**

## 3. 功能需求(逐条填)
- [ ] 音频采集(麦克风:型号 / 接口 / 采样率)——**待定**
- [ ] 音频播放(喇叭 / 功放 / 接口)——**待定**
- [ ] 无线传输(射频方案 / 协议 / 码率 / 加密)——**待定**
- [ ] 交互(PTT 按键、状态指示、显示?)——**待定**
- [ ] 其它(录音 / 频道切换 / 配对…)——**待定**

## 4. 非功能需求
- 延迟、续航、距离、音质等量化指标 ——**待定**

## 5. 验收标准(怎样算完成)
- 最小成功标准(MVP)——**待定**(例:两台设备 PTT 互通、可懂语音、端到端延迟 < X ms)

## 待确认(谈到就移到上面对应小节)
1. **射频方案**:ESP-NOW / WiFi(UDP)/ BLE / 802.15.4(Thread/Zigbee)/ 外接 LoRa?各自在音频码率与距离上的取舍待估算。*(板载射频已实测:WiFi2.4G/BLE/802.15.4 均可用,无 Classic BT,LoRa 需外接 —— FACTS F‑1。)*
2. **音频硬件**:Unit-C6L 本体有无麦克风 / 喇叭?需外接哪些模块(I2S MEMS mic、I2S/PWM 功放)?
3. **音频参数**:采样率、位深、编解码(原始 PCM / ADPCM / Opus?ESP32-C6 算力能否实时编码?)。
4. **拓扑**:点对点固定配对,还是多台共享频道。
5. **最小成功标准**与优先级排序。
