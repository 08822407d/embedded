# DECISIONS — 关键决策台账(只追加)

> 每条:日期 / 决定了什么 / 为什么 / 否决了什么。**已决之事不在别处偷偷推翻**;要改就新追加一条说明"取代 #NNN"。

## #000 — 2026-06-21 — 建立持久化体系
- **决定**:沿用 `Atom_Roller_Bala` 已验证的 `CLAUDE.md + docs/{REQUIREMENTS,DESIGN,DECISIONS,FACTS,STATUS,HANDOFF,OPERATIONS}.md` 模式,按对讲机项目改写。
- **为什么**:Claude Code 对话上下文易失(清空 / 超限截断 / 移动目录 / 误删),真相必须落盘 `docs/` 并入 git 才可恢复;该模式已在另一项目实战验证。
- **否决**:不沿用更早被删的 matrix / agent-context 模板 —— 它混乱,且曾因"用删文件的方式做备份"把整套文档弄丢。**备份绝不用删文件的方式**。
- **配套红线**:未经本会话明确指示不编码 / 不烧录 / 不持续发射射频(见 CLAUDE.md)。

## #001 — 2026-06-21 — LoRa 测试:有界运行+自动关射频、等串口再发
- **决定**:LoRa 测试固件**有界运行**(PINGER 发满 N=30 包 / PONGER 空闲 20s 超时)后 `radio.sleep()` 关射频;**等 USB-CDC 串口打开(带 30s 超时)再开始打印与收发**;主机抓取脚本先挂载后复位、对齐 t=0。
- **为什么**:(a)兜底——不让设备测试后持续对外发射射频(呼应红线);(b)原生 USB-CDC 一次性 boot 日志竞态(F‑13),不对齐就漏 banner / 早期包,等串口 + 先挂载后复位双保险确保抓全。
- **否决**:无限循环发射(违红线、且无明确"测试结束"信号)。

## #002 — 待定
- *(对讲机的射频方案、音频编解码等关键选型谈定后追加)*
