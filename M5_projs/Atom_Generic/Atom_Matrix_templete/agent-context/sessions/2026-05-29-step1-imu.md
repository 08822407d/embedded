# 2026-05-29 — 第 1 步：IMU 姿态角读取与标定

## 本次进展
- 验证开发环境：PlatformIO 6.1.18，平台 espressif32@54.3.20（ESP32 Arduino 3.2.0），库均最新。
- 串口：开发板 = `/dev/ttyUSB0`（FTDI, "M5stack"），115200。
- 搭 `tools/serial_bridge.py`：单进程独占串口、读线程写日志 + FIFO 发命令，支持并发收发
  （`pio device monitor` 在无 TTY 的 Agent 环境不可用）。
- 搭 `agent-context/` 持久化上下文存储 + 根目录 `CLAUDE.md` 引导入口。
- 写全新 `src/main.cpp`：读 MPU6886 → atan2 换算 pitch/roll，50ms/20Hz 输出。编译/烧录/监控通过。
- 完成三状态标定，定论：**可动轴 = pitch，平衡点 pitch₀≈0°，θr≈±30°，roll 恒定无关**。
  详见 [system-model.md](../system-model.md)、[decisions/001](../decisions/001-imu-readout.md)。

## 当前状态
- 板上固件 = 本工程第 1 步（IMU 姿态输出），原电机控制固件已被覆盖（无源码）。
- 串口桥常以后台方式运行，日志在 `tools/serial.log`，命令 FIFO `tools/serial.cmd`。
- `agent-context/` 已写入：requirements/001、system-model、decisions/001、glossary、本会话。

## 未完成 / 待办
- [ ] 反作用轮（Roller）电机命令协议 → 写入 `protocols/`（拿到前不驱动电机）。
- [ ] pitch 加陀螺仪融合（互补/卡尔曼），消除动态下运动加速度干扰。
- [ ] 是否 `git add` 提交 CLAUDE.md / agent-context / tools / src / platformio.ini（待用户定）。

## 下一步
等用户给下一步指令（多半是电机协议或姿态融合）。

## 备注 / 坑
- Agent 环境**无 TTY**：`pio device monitor` 不可用；前台 `sleep` 被禁用（用 `timeout` 替代）。
- 烧录前需先 `pkill -f serial_bridge.py` 释放串口（被 SIGTERM 终止时退出码 144 属正常）。
- 安全：电机/平衡系统，未拿到协议、未经授权不发会让电机动作的命令。
