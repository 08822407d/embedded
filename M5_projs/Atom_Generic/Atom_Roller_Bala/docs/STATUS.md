# STATUS — 当前状态(恢复锚点)

> 这是新会话的第一落点。**只记"现在"**;历史交给 `git log`,别在这里堆。
> 每段工作收尾必须更新本文件并 `git commit`。

- **更新时间**:2026-06-20
- **当前阶段**:需求 / 接口闭环,控制律定为 **PID 先行** —— 准备进入 PID 控制设计

## 进行中
- 接口、供电、I2C 总线分配全部钉死;控制律路线已定(先 PID、后 LQR)。
- 待开展:PID 控制设计(含选定 RollerCAN I2C 模式:电流/力矩?)+ 平衡环 + 飞轮卸荷。

## 已完成
- 持久化体系与接手协议(DECISIONS #000)。
- 系统定义:平面反作用轮倒立摆;硬件 = M5Atom(板载 MPU6886)+ RollerCAN。
- 机械几何锁定(楔顶 120°、平衡态斜面 30°、静置态中轴对桌面 60°、恢复区 ±30°;标称)。
- 术语定稿(恢复区 / 静置态 / 起跳);最小成功 = θ=0 且飞轮、机体角速度 ≈ 0(不要求抗扰)。
- 电气钉死:供电 = 外部 12V→RollerCAN CAN 口(F‑13);I2C 总线 = IMU `Wire1` / RollerCAN `Wire`(Grove 26/32)(F‑12、F‑14)。
- 控制律决策:先 PID、后 LQR(DECISIONS #001)。
- RollerCAN `UnitRollerI2C` API 摸底(DESIGN §5.1);事实台账 FACTS F‑1~F‑14。
- 侧视示意图 `docs/assets/balance-geometry-sketch.png`。

## 下一步
- [ ] **PID 控制设计**:选定 RollerCAN I2C 模式(反作用轮多用电流/力矩)、平衡环结构、飞轮卸荷。
- [ ] 物理参数测量(质量 / 质心距 / 机体与飞轮转动惯量 / 最大力矩)→ 进 FACTS C 节。
- [ ] 拆第一批 Codex 任务:I2C 打通 `UnitRollerI2C` → `M5.IMU` 读 θ → 最小平衡环。

## 卡点 / 待澄清
- 见 REQUIREMENTS 文末「待确认」(RollerCAN 模式、I2C 地址/速率、物理参数、真机标定、环路频率 vs I2C)。

## 交给 Codex 的状态
- 暂无(等 PID 设计与接口细节钉死;且需用户明确指示才能开始实现 / 烧录 —— 见 CLAUDE.md 操作红线)。

## 开放问题
- **理想 vs 真机**:讨论中的角度均为标称值;真机姿态解算有微小偏差 → 平衡设定点与各阈值需测试标定、留裕度。
- RollerCAN 速度 / 位置 / 电流的物理标度待标定;控制环频率受 I2C 往返延迟约束待实测。
