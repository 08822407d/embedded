# STATUS — 当前状态(恢复锚点)

> 这是新会话的第一落点。**只记"现在"**;历史交给 `git log`,别在这里堆。
> 每段工作收尾必须更新本文件并 `git commit`。

- **更新时间**:2026-06-19
- **当前阶段**:需求 / 宏观设计已闭环 —— 准备进入控制设计

## 进行中
- 需求层宏观项已全部与用户确认(系统形态、机械几何、电气接口、最小成功目标、术语、范围与约束)。
- 待开展:控制模式选型(速度/位置/电流)+ 控制律的专题讨论。
- 📌 **今日(2026-06-19)需求对接告一段落;明日从「下一步」第 1 条(控制模式选型)继续。**

## 已完成
- 持久化体系与接手协议(DECISIONS #000)。
- 系统定义:平面反作用轮倒立摆;硬件 = M5Atom(板载 MPU6886)+ RollerCAN,I2C 连接,USB‑C 有线开发。
- 机械几何锁定(楔顶 120°、平衡态斜面 30°、静置态中轴对桌面 60°、恢复区 ±30°;均为标称值)。
- 术语定稿:**恢复区 / 静置态 / 起跳**;最小成功 = θ=0 且飞轮、机体角速度 ≈ 0(不要求抗扰)。
- RollerCAN 库 `UnitRollerI2C` API 摸底(DESIGN §5.1);事实台账 FACTS F‑1~F‑11。
- 侧视示意图 `docs/assets/balance-geometry-sketch.png`。

## 下一步
- [ ] **控制模式选型专题研究**(速度/位置/电流,平衡环用哪种)。
- [ ] 控制律选择(PID / LQR)+ 飞轮卸荷(防转速饱和)。
- [ ] 物理参数测量(质量 / 质心距 / 机体与飞轮转动惯量 / 最大力矩)→ 进 FACTS C 节。
- [ ] 拆第一批 Codex 任务:I2C 打通 `UnitRollerI2C` → `M5.IMU` 读 θ → 最小平衡环。

## 卡点 / 待澄清
- 见 REQUIREMENTS 文末「待确认」(控制模式、I2C 细节、物理参数、真机标定)。

## 交给 Codex 的状态
- 暂无(等控制模式与接口细节钉死;且需用户明确指示才能开始实现 / 烧录 —— 见 CLAUDE.md 操作红线)。

## 开放问题
- **理想 vs 真机**:讨论中的角度均为标称值;真机姿态解算有微小偏差 → 平衡设定点与各阈值需测试标定、留裕度。
- RollerCAN 速度 / 位置 / 电流的物理标度待标定。
