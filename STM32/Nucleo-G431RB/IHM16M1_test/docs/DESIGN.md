# DESIGN — 架构与当前设计

> 本文件是 **Codex 实现代码的依据**:写清楚"系统怎么搭、模块怎么分、接口长什么样、关键参数取值",让 codex 照着写 `src/`。
> 设计变化要**同步回写这里**,并在 [`DECISIONS.md`](DECISIONS.md) 追加理由。需求未定前,本文件保持骨架。

## 0. 现状
**需求与工具链尚未确定,暂无设计。** 待 [`REQUIREMENTS.md`](REQUIREMENTS.md) 落定后填充。

## 1. 系统架构(待填)
- 框图 / 数据流 / 控制环结构(电流环 / 速度环 / 位置环)
- 工具链与代码生成方式(CubeMX/MCSDK 生成 + 手写,还是裸 HAL)

## 2. 模块划分(待填)
- 各模块职责、文件位置、对外接口

## 3. 关键外设映射(待填,核实后引用 FACTS.md)
- TIM(PWM 互补输出 + 死区)、ADC(相电流 / 母线采样)、运放、GPIO(使能 / 故障)、串口(调试 / 上位机)

## 4. 关键参数(待填)
- PWM 频率、死区、采样时序、控制环频率、保护门限、标幺 / 单位约定

## 5. 给 Codex 的实现约束(通用,先立)
- **默认安全**:上电 / 复位后 PWM 输出占空为 0、驱动级禁用,需显式命令才使能;任何故障(过流 / 过温 / 欠压)立即关断输出。
- **引脚 / 外设以 [`FACTS.md`](FACTS.md) 为唯一真值**,别从记忆臆测;没核实的先停下问。
- 串口名 / 探针名**先探测别写死**(见 [`OPERATIONS.md`](OPERATIONS.md))。
