# REQUIREMENTS — 需求(做什么 / 为什么 / 怎样算完成)

> 规则:已确认的写成**肯定句并标日期**;未确认的放「待确认」区,**别当既定事实用**。

## 1. 目标(一句话)
_待用户说明。_

## 2. 硬件平台(已知,2026-06-29)
- **主控**:STM32 **Nucleo-G431RB** —— MCU = STM32G431RB,Cortex-M4F @170MHz,带 FPU + CORDIC + 高分辨率定时器,ST 主推的电机控制 MCU。
- **扩展板**:**X-NUCLEO-IHM16M1** —— ST IHM 系列**三相电机(BLDC/PMSM)驱动评估板**,板载电流采样、可配三电阻 / 单电阻采样,适配 ST MCSDK FOC。
  > 具体驱动芯片、母线电压范围、采样电阻、跳线配置等**待核实后写入 [`FACTS.md`](FACTS.md)**,此处不臆断细节。
- **组合典型用途**:三相 BLDC/PMSM 电机的 FOC / 六步控制(待用户确认实际目标)。

## 3. 功能需求
_待填(用户说明需求后逐条列,每条标 `[ ]`/`[x]`)。_

## 4. 非功能需求
_待填(实时性、控制频率、安全保护——过流/过压/堵转、精度等)。_

## 5. 验收标准(MVP)
_待填(怎样算"做完"——可观测、可复现的判据)。_

## 待确认
- 项目**具体用途 / 控制目标**(调速?定位?力矩?仅外设点亮验证?)
- 是否使用 **ST MCSDK(FOC)** 生成,还是自研控制算法
- **电机型号 / 参数**(极对数、额定电压电流、KV)、供电方案
- 是否有**位置 / 速度反馈**(霍尔、编码器、还是纯无感)
- 安全要求(急停、过流过温保护门限)
