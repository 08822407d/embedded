# Codex 任务书 — 判定 X-NUCLEO-IHM16M1 采样拓扑(单电阻 / 三电阻),啃手册 + 截图给用户对照

> 给 Codex:读完即可自主执行,**但含"人在环(HITL)"交互点**(见 §-1),遇须用户手动/授权处必须停下等回复。
> 项目根(**本机 Windows**)= `C:\Users\cheyh\Desktop\embedded\STM32\Nucleo-G431RB\IHM16M1_test`(monorepo `embedded` 内)。
> 真相以仓库 `docs/` 为准:硬件事实 `docs/FACTS.md`、路线 `docs/DECISIONS.md` #003、双机同步 `docs/SYNC_WORKFLOW.md`、Windows 冒烟基线 `docs/HW_SMOKE_TEST_WIN.md`。**动手前先读它们。**

## 目标(一句话)
从 **UM2415**(X-NUCLEO-IHM16M1 用户手册)查清:该板**单电阻 / 三电阻电流采样**是由**哪些跳线 / 0Ω 电阻**决定的、**出厂默认是哪种**、**如何在实物板上辨认**;并把手册里相关的**跳线表 / 电流采样原理图 / 板卡俯视布局图**裁成**清晰截图文件**交给用户,让用户**对照实物板**确认当前配置。**不改硬件、不烧录、不转电机。**

## ⛔ 红线(最高优先,违反即停)
- **只读 + 离线分析为主**:允许①联网**下载 UM2415 PDF**、②把 PDF 页面**渲染成图片并裁剪**、③只读读取用户提供的板卡照片、④写分析文档。
- **绝对禁止**:烧录/下载固件、擦写、复位运行、任何驱动电机/输出 PWM 的动作;**启动 MC Workbench / Motor Profiler / MotorPilot 等会驱动硬件的功能**(只允许**只读读取** MCSDK 安装目录内的板卡定义文件,不启动 GUI 去连板子)。
- **安装限制**:本项目 ST 级工具安装由用户主导,不擅自装。**唯一允许**:为渲染 PDF 而装一个**纯 Python wheel 包**(如 `pymupdf`),但**必须走 §0/§-1 让用户批准**后再装;若用户不同意,改用本机已有工具或让用户帮忙导出页面。
- 任何一步若必须写/烧/转电机/改板才能继续 → **停,走 §-1 问用户或写报告等待**。

## §-1 交互协议(HITL,务必遵守)
遇到**须用户手动或授权**的步骤(联网下载、装 pip 包、需要用户看实物板反馈),**停下等用户**,固定输出:
```
==== 需要你手动操作 [HITL-n] ====
目的:<为什么>
请你做:
  1) <具体动作>
  2) ...
做完请回复:"HITL-n 完成"(或反馈实际结果/看到什么)
在你回复前我不继续、不猜测。
====
```
**本任务的关键 HITL**:图截好后,**把每张截图的文件路径列给用户,请用户打开对照实物板**,回答"当前跳线插在哪/是几电阻",你据其反馈定论。合并打断、别零碎追问(呼应仓库"少打断")。

## §0 权限预检(先把会弹窗的动作各探一个最小实例)
- 网络:`curl -sSI https://www.st.com`(确认能出网;UM2415 下载见 §2)
- (若需)PDF 渲染库:探测本机是否已有可用工具(Python `fitz`/`pdf2image`、`pdftoppm`、Ghostscript);**若都无、需 `pip install pymupdf`,走 HITL 让用户批准**。
- 文件写:在项目内新建 `docs/IHM16M1_SHUNT_CONFIG.md` 与图片输出目录。
先列出你判断会弹窗的动作,逐项探通,再进 §3。

## §1 已验证事实(直接用;来源见 docs/FACTS.md 及用户 2026-07-03 实拍)
- 板 = **X-NUCLEO-IHM16M1**,功率级 = **STSPIN830**,支持**单 / 三电阻**电流采样,板上跳线选(F-8)。核心手册 **UM2415**(F-7)。
- **霍尔连接已确认**(用户实拍):板上霍尔座 **J1**,丝印 `GND · +5V · C+H3 · B+H2 · A+H1`;电机霍尔 5 线(VCC/GND/H1/H2/H3)已插入(H1→A+H1、H2→B+H2、H3→C+H3)。
- **三相 U/V/W** → 板左侧**绿色 3 位螺钉端子**;**电源** → 板右侧绿色 2 位端子或黑色 DC 桶插座。
- **实拍照片在磁盘**:`C:\Users\cheyh\Pictures\IMG_0920.png`(约 2865×2865,IHM16M1 俯视,含各跳线现态)——可读取/裁剪辅助定位;**注意**:超 2000px 的图需先缩放再处理。
- 板上**可见跳线**(供你在手册里定位对应项):① 左上角靠 `FOR EVALUATION / NOT FCC / GND TP2` 附近一组竖排跳线;② 中部 `ROFF` 丝印旁一个跳线;③ 霍尔座 J1 右侧 `J3` 附近一个跳线。**别据此猜结论,以手册为准。**
- 目标路线是**霍尔 FOC**(#003)。**顺带核实并在报告里说明**:MCSDK 对本板做霍尔 FOC 时,单电阻是否可用、三电阻是否更优——用来**校正**仓库里"霍尔 FOC 必须三电阻"这句可能偏绝对的表述(不要照抄旧结论)。

## §2 你(codex)自己查(别让 CC 代查)
- **获取 UM2415 PDF**:从 st.com 下载 X-NUCLEO-IHM16M1 的用户手册 UM2415(“Getting started with the X-NUCLEO-IHM16M1 … STSPIN830 …”)。记录准确 URL 与版本/日期。
- 在 UM2415 里定位**电流采样 / current sensing / shunt configuration** 章节:找出**单电阻 vs 三电阻**由**哪些跳线(Jx)/ 0Ω 电阻(Rx)** 决定、**每种配置的具体插法**、**出厂默认**;并找到**电流采样原理图**与**板卡俯视布局图/丝印图**。
- **PDF → 图片**的可行渲染方式(本机已有工具优先;必要时经 HITL 装 `pymupdf`)。把相关页/区域裁成**清晰、文字可读**的图片。

## §3 执行阶段(按序;遇手动/授权走 §-1)
0. §0 权限预检。
1. 下载 UM2415,记 URL/版本。
2. 定位采样拓扑相关章节(跳线表 + 原理图 + 布局图)。
3. 渲染并裁剪出**清晰截图**:至少 (a) 单/三电阻**跳线配置表**、(b) **电流采样原理图**、(c) **板卡俯视布局图**(标出这些跳线位置)。图存到输出目录,文件名见名知意。
4. 写分析:哪种跳线=单电阻、哪种=三电阻、**默认**是哪种、**在实物板上怎么辨认**(对应 §1 里三处可见跳线,尽量点名 Jx);并给出"用户该在实物上看什么、怎么判断当前是几电阻"的核对清单。
5. **HITL**:把截图路径 + 核对清单交给用户,请其对照实物板反馈当前配置;据反馈定论,回写文档。

## §4 交付目标(测什么 / 产出什么)
1. **截图文件**(清晰可读):单/三电阻跳线表、电流采样原理图、板卡布局图。
2. **书面判读**:单/三电阻各自跳线插法 + 出厂默认 + 实物辨认法 + 与 §1 三处可见跳线的对应。
3. **HITL 对照**:用户依截图确认实物当前是**单电阻还是三电阻**,并记录其反馈。
4. **附带结论**:本板霍尔 FOC 下单电阻是否可用、三电阻是否更优(校正旧表述)。
5. 任一步查不到/不确定 → 如实记录(缺什么、卡在哪),不臆断、不越红线。

## §5 产出与落盘
- 分析写入**新增** `docs/IHM16M1_SHUNT_CONFIG.md`(含结论、跳线映射、实物辨认法、UM2415 出处页码、HITL 记录)。
- 截图存到 `hw_ref/ihm16m1/`(或类似);**大文件 / PDF 原件 `.gitignore` 掉**(别把整本 UM2415 或大图塞进仓库),仅在文档里给出 URL + 页码 + 本地路径。若截图小且必要可入库,自行权衡。
- 核实到的事实(默认采样拓扑、跳线映射、单电阻可行性)→ 追加 `docs/FACTS.md`(**接续现有最大 F 编号**,带来源+日期,**不改旧条目**);必要时在 `REQUIREMENTS.md`/`DECISIONS.md` 补一条说明"校正霍尔 FOC 采样拓扑表述"。
- 更新 `docs/STATUS.md`。
- **收尾 = commit + push**(SYNC_WORKFLOW:不 push 到 origin,另一台机看不到)。

## §6 纪律 / 边界
- 红线见顶部:只读/离线分析,不烧/不擦/不 run/不转电机/不启动 MCSDK 驱动功能;仅允许下载 PDF+渲染+读板卡定义文件;pip 装包仅限 PDF 渲染且经用户批准。
- HITL 铁律:须人动手/授权处必停等,不替用户做、不假设已做(§-1)。
- 改动限本项目目录,别碰 monorepo 其它项目;Windows 路径含空格加引号;别乱设 `core.autocrlf`。
- 不确定就停下问,别为"给结论"而臆断——**以 UM2415 原文为准**。
