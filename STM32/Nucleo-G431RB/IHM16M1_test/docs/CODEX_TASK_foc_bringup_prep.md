# Codex 任务书 — 正式 FOC 首次带电前的离线审计与降险准备(不烧录 · 不碰硬件 · 不转电机)

> 给 Codex:读完即可自主执行。**本任务全程离线**——只读生成工程的源码/配置 + 写分析文档;**绝不烧录、绝不访问硬件、绝不驱动电机、绝不启动 MC Workbench/MotorPilot 的驱动功能。**
> 项目根(Windows)= `C:\Users\cheyh\Desktop\embedded\STM32\Nucleo-G431RB\IHM16M1_test`(monorepo `embedded` 内)。
> 真相以 `docs/` 为准:`FACTS.md`(尤其 F-24/F-28/F-29/F-30/F-31)、`FOC_FW_BUILD.md`、`DECISIONS.md` #003、`GITIGNORE_AND_REGEN.md`、`MOTOR_PROFILER_SOP.md`、`SYNC_WORKFLOW.md`。**动手前先读。**

## 背景与本任务定位(务必理解)
- 路线 #003 已推进到:**正式霍尔 FOC 工程 `GM16020-06_foc` 生成 + 离线 Debug 构建通过 + 配置审计**(见 `FOC_FW_BUILD.md`)。项目**主动暂停在"烧录 / 首次闭环上电"之前**。
- **下一步的"首次闭环带电"是最危险的一步**:霍尔方向/相序/Ke 任一不对,电机可能**不受控冲高速或剧烈抽搐**;且**本机供电无可调限流、无保险丝**(F-25),风险更高。
- **那一步是"用户操作 GUI/硬件 + CC 旁站"的红线操作,不是 codex 任务。** 本任务是它的**离线前置准备**:把能在源码/配置层面查清、定好的都做掉,**为带电降险**,产出一份"带电前审计与建议"喂给后续现场 SOP。
- 一句话:**你负责把首次带电"该看什么、该限多低、哪里有雷、Ke 怎么办"在离线阶段研究透并写下来;真去带电由人来做。**

## 目标
产出一份**首次带电前的离线审计 + 降险准备**,包含:
1. **Ke 量化项定论**:0.449→生成 0.4 到底影不影响霍尔 FOC 控制,给出"接受/重生成/实测校"的明确建议。
2. **霍尔配置深查**:生成工程的霍尔方向/相序/placement 是否与我们实测(F-23 序列、58°/120°)自洽;列出"首次带电若霍尔方向/相序错"的症状与修法。
3. **故障保护与安全态审计**:过压/欠压/过流/过温/刹车、上电默认态、有无自动启动。
4. **保守首测参数集**:首次带电应把电流/转速/斜坡限到多低,以及**每一项在运行时经 MotorPilot/MCP 可改还是须重生成**。
5. **遥测观察方案**:首测时用 MotorPilot 看哪些量判断"对不对"(不靠已拆的指针)。
6. **风险台账**:逐条列雷点 + 触发条件 + 缓解/应对。

## ⛔ 红线(最高优先,违反即停)
1. **全程离线**:只允许**只读**生成工程源码/`.ioc`/头文件、只读 MCSDK 安装内的板卡定义/文档、写本项目 `docs/`。
2. **绝不**:烧录/下载、擦写、复位运行、枚举/连接板卡、跑 STM32_Programmer_CLI 做写操作、启动 MC Workbench/MotorPilot 去连板或驱动电机、给电机母线上电。
3. **绝不手改生成工程里"控制关键参数"**(电流/速度限、PI、Ke、霍尔角等):要改这些属**重生成(MC Workbench GUI)**范畴,GUI 是用户的活,你只**给出建议设置**,不自己改、不自己重生成。若为分析需临时读取/试算,不得落到生成树里改文件。
4. 若某结论**必须靠带电实测**才能定 → **不实测**,明确写"须首次带电时验证",交给现场 SOP。
5. 任何越出"离线只读分析"的需求 → 停,走 §-1 ESC 交回 CC。

## §-1 交互协议(HITL + ESC)
- **HITL**(须用户手动/授权,本任务应极少):按格式停等。
- **ESC**(超出离线分析职责、或需硬件/决策):停并交回 CC——
```
==== 需 CC 决策 [ESC-n] ====
背景/卡点:... / 我已查:...(附证据) / 需要 CC 定的:...
====
```

## §0 权限预检(只读为主)
- 文件读:生成工程 `GM16020-06_foc/`(被 gitignore,但**在磁盘上、可只读**)、MCSDK 安装目录只读。
- 若查 Ke/寄存器语义需查 MCSDK 文档(本机 `.../MC_SDK_6.4.2/Documentation/`,只读)或联网 ST 文档(`curl -sSI` 探针,如需)。
- 文件写:仅 `docs/`。
列出会弹窗的动作,探通再进 §3。**不做任何写硬件/连板动作。**

## §1 已确认事实(直接用)
- **电机标定(F-28)**:Rs=1.41Ω、Ls=0.19mH、极对数=4、Ke(BEmf)=0.449(⚠️ run 间 0.385~0.62)、maxRatedSpeed≈19806、SM-PMSM;**霍尔 placement=58°、displacement=120°**;实测霍尔有效序列(F-23)`010→011→001→101→100→110→010`,`h1_rise=4`。
- **FOC 工程有效配置(FOC_FW_BUILD.md / F-30/F-31)**:主传感器=Hall、TIM3 PC6/7/8;PWM=TIM1 30kHz,U/V/W 高侧 PA8/9/10、使能 PB13/14/15、刹车 PA11/TIM1_BKIN2;三电阻双 ADC(U=PA1/ADC1_IN2、V 共享 PB11、W=PA7/ADC2_IN4)、shunt 0.33Ω、gain 1.53;母线检测 PA0、温度 PC4;OV=15V、UV=7V、过压动作=关 PWM;默认 Speed 模式目标 500rpm、自算 PI;potentiometer/start-stop/ESC/Motor Profiler 全关;**源码无自动 `MC_StartMotor1`**;MCP over USART2 PA2/PA3 1843200。
- **Ke 量化(F-31/FOC_FW_BUILD "Ke Quantization")**:种子 0.449,生成 `.ioc`/`pmsm_motor_parameters.h`/motor config register 为 **0.4**;codex 上次查到 0.4 进"电机配置寄存器/上报"而**未见于霍尔 FOC 电流/速度控制路径**——本任务要**把这条坐实并给建议**。
- 供电无可调限流、无保险丝(F-25);母线范围 7–45V(F-29)。
- **指针已拆、首次带电前保持拆除**(高速会飞);首测判方向靠慢动作/遥测,不靠指针。

## §2 你(codex)自己查
- **Ke 在 MCSDK 霍尔 FOC 里的确切用途**:`pmsm_motor_parameters.h` 的 `MOTOR_VOLTAGE_CONSTANT` / 由 Ke 派生的量,进哪些计算(BEMF 观测器?电压前馈解耦?仅上报寄存器?)。霍尔作主传感器时位置来自霍尔,判定 0.4 vs 0.449 对**实际控制**有无影响、影响多大。
- **哪些控制参数运行时经 MotorPilot/MCP 可改**(速度目标、Iqmax/电流限、斜坡、PI…)vs **哪些烧进固件须重生成**。给出逐项"改法(运行时寄存器名 / 或须 WB 重生成)"。
- **MotorPilot 首测可观测/可记录的量**(霍尔态、电角度、实测转速、Iq/Id、Vbus、故障位)——列出注册表/界面位置。
- 生成代码里**霍尔期望的相序/方向**与我们 F-23 实测序列是否一致的判据(能静态看到多少、哪些只能带电验)。

## §3 任务(离线,逐项产出结论)
**A. Ke 量化定论**:坐实 0.4 的传播路径;结论=对霍尔 FOC 控制"无影响/有影响";建议三选一并说明:①接受 0.4、首测实测校;②回 WB 用更高精度重生成(给出该在 WB 哪里设);③其它。并把 run 间 Ke 跳动(0.385~0.62)的处置一并给建议。
**B. 霍尔配置深查**:核对 placement 58°/displacement 120°/相序方向在生成代码里的落地;明确"首次带电若霍尔方向反 / 相序错"的**症状**(如原地抽搐、反转、启动失败、过流)与**修法**(交换两相 or 两霍尔 or 改 placement/方向标志,分别代价),写成现场可照做的判分支。
**C. 故障保护与安全态审计**:确认 OV15/UV7/过流/过温/刹车 PA11 生效、阈值与我们 12.6V 母线匹配;确认上电默认态(输出禁用、无自动启动);指出任何"首测前该确认生效"的保护。
**D. 保守首测参数集**:给出首次带电建议值(**Imax/电流限压到很低,如 ≤0.3–0.5A**;速度目标低,如 100–300rpm;斜坡放缓;确保故障保护开),每项标注"运行时 MCP 可改 / 须重生成";优先运行时可改的方案(免动生成树)。
**E. 遥测观察方案**:列首测时该盯的量 + 在 MotorPilot 哪看 + "正常 vs 异常"判据(不靠指针)。
**F. 风险台账**:雷点(无限流/无保险丝、Ke 存疑、霍尔方向未带电验、低电感高纹波、tiny 电机易过速…)× 触发条件 × 缓解/应对(含"立即断电"判据)。

## §4 交付与落盘
- 新增 `docs/FOC_BRINGUP_PREP.md`:A–F 全部结论,**结构清晰、可被现场 SOP 直接引用**;Ke 定论、霍尔症状/修法分支、保守参数表(带"运行时/重生成"标注)、遥测清单、风险台账。
- 新确认事实(Ke 传播路径结论、运行时可改参数清单等)→ 追加 `FACTS.md`(接续 F 编号,带来源+日期,不改旧条目)。
- 更新 `STATUS.md`(反映"首次带电前离线准备已完成,待现场 SOP + 用户授权带电")。
- 若审计中发现**生成工程的真实问题**(非 Ke 量化的、会影响安全/正确性的配置错)→ 醒目写入报告 + `ESC` 提醒 CC,不自行改。
- **收尾 = commit + push**(SYNC_WORKFLOW)。

## §5 明确不做(再次强调)
- 不烧录、不连板、不上电、不转电机、不启动 MotorPilot/MC Workbench 驱动功能。
- 不手改生成工程控制关键参数、不自行重生成(那是 WB GUI = 用户的活;你只给建议设置)。
- 不做任何"只能带电才能定"的结论——标注留给现场。

## §6 纪律 / 边界
- 红线见顶部;有疑走 ESC 交回 CC,别越"离线只读分析"。
- 改动限本项目目录 `docs/`;端口/SN 现场探测别写死;Windows 路径含空格加引号;别乱设 `core.autocrlf`。
- 允许你自行设计更周全的审计方法,但**结论要有源码/文档证据、并完整留痕**,便于 CC 复盘与现场照用。
