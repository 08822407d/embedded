# 跨 OS(Linux ⇄ Windows)统一换行:仓库内一律存 LF,检出按平台
* text=auto eol=lf

# Windows 专用脚本保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二进制(别做换行/diff 处理)
*.elf binary
*.bin binary
*.hex binary
*.pdf binary
*.ioc text eol=lf
